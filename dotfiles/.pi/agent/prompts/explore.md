---
description: Explore a task/question with the fast project scout subagent and return compressed handoff context
argument-hint: "<task or question>"
---
Use the `subagent` tool with `agentScope: "project"`.

Delegate this task to `scout`:

> $ARGUMENTS

Ask it to return the scout handoff format: Files Retrieved, Commands Run, Key Code / Resources, Architecture, Risks / Unknowns, and Start Here.
