---
apiVersion: custom.sandboxes.x-k8s.io/v1alpha1
kind: Application
metadata:
  name: slingshot
  namespace: default
spec:
  image: busybox:latest
  command: "tail -f /dev/null"
  port: 3000
  hostname: "slingshot.${SECRET_DOMAIN}"
