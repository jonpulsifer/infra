---
apiVersion: helm.toolkit.fluxcd.io/v2beta1
kind: HelmRelease
metadata:
  name: metallb-crds
  namespace: flux-system
spec:
  interval: 24h
  chart:
    spec:
      chart: ./charts/metallb/charts/crds
      version: ''
      sourceRef:
        kind: GitRepository
        name: metallb-crds
        namespace: flux-system
      interval: 24h
