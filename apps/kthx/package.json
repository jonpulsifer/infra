{
  "name": "kthx",
  "private": true,
  "version": "2.0.0",
  "type": "module",
  "bin": {
    "kthx": "cli/main.ts"
  },
  "scripts": {
    "start": "bun run server/index.ts",
    "pack": "bun run pack.ts",
    "lint": "biome check .",
    "lint:fix": "biome check . --write",
    "typecheck": "tsc --noEmit",
    "test": "bun test"
  },
  "dependencies": {
    "@repo/archive": "workspace:*",
    "@repo/kthx": "workspace:*"
  },
  "devDependencies": {
    "@repo/typescript-config": "workspace:*",
    "@types/bun": "1.4.0",
    "typescript": "5.9.3"
  }
}
