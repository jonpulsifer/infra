# `bun run dev` compiles src/web/client/index.html through Bun.serve's HTML
# import, so it needs the Tailwind plugin that build.ts passes to Bun.build.
[serve.static]
plugins = ["bun-plugin-tailwind"]
