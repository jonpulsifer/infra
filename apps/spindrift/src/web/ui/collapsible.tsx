/**
 * Radix's collapsible, for a controlled `open`: React cannot take back a
 * `<details>` element's state once the reader has toggled it.
 */
export {
  Content as CollapsibleContent,
  Root as Collapsible,
  Trigger as CollapsibleTrigger,
} from '@radix-ui/react-collapsible';
