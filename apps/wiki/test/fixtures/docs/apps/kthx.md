---
title: kthx
description: A deploy platform for built apps.
status: live
---

kthx deploys apps. See [built apps](kthx/built-apps.md) and [the network](../platform/network.md#two-sites).

## Before you start

> [!NOTE]
> Read the runbook first.

> [!WARNING]
> Multi-line
>
> warning.

> A plain quote.

## Install

```sh
nix run .#<hostname> -- switch
curl -s localhost/mcp | jq '.result.tools | length'
```

## Install

The second Install heading gets a -1 id.

### Ünïcode héading — with: punctuation! (yes)

Inline `<host>.lolwtf.ca` and raw <b>tags</b> stay text & escaped.

An autolink <https://example.com/docs> works, and <img src=x onerror=alert(1)> stays text.

#### Deep detail

An h4 gets an id but stays out of the rail.
