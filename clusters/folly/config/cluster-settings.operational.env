# Operational (non-network) knobs for the folly cluster.
# Hand-edited. Merged with topology/topology.json by topology/generate.sh
# to produce cluster-settings.yaml. Network facts live in topology.json, not here.
TIMEZONE=America/Halifax
CERT_CLUSTER_ISSUER=letsencrypt-production
