apiVersion: cilium.io/v2alpha1
kind: CiliumPodIPPool
metadata:
  name: default
spec:
  ipv4:
    cidrs:
      - ${CILIUM_POD_CIDR}
    maskSize: 24
---
apiVersion: "cilium.io/v2alpha1"
kind: CiliumLoadBalancerIPPool
metadata:
  name: fml
spec:
  blocks:
    - cidr: ${LB_RANGE}
  serviceSelector:
    # Selects every Service: nothing is labelled debug=lol.
    matchExpressions:
      - { key: debug, operator: NotIn, values: [lol] }
