---
# Throwaway: tests whether the Tailscale operator's L7 Ingress injects
# identity headers on this tailnet. Delete it once the answer is recorded.
apiVersion: v1
kind: Namespace
metadata:
  name: identity-spike
  labels:
    app.kubernetes.io/name: identity-spike
    pod-security.kubernetes.io/enforce: restricted
    pod-security.kubernetes.io/enforce-version: latest
