---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: hub-env
  namespace: hub
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: onepassword-connect
  target:
    name: hub-env
    creationPolicy: Owner
  data:
    - secretKey: TEMPESTWX_TOKENS
      remoteRef:
        key: qytxxks3t6ttmhkbfl43ginglu
        property: password
