---
apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: tailscale-connectors
  namespace: flux-system
spec:
  interval: 1h0m0s
  path: ./clusters/offsite/networking/tailscale-connectors
  prune: true
  sourceRef:
    kind: GitRepository
    name: infra
  dependsOn:
    - name: tailscale
  decryption:
    provider: sops
    secretRef:
      name: sops-age
  postBuild:
    substituteFrom:
      - kind: ConfigMap
        name: cluster-settings
      - kind: Secret
        name: cluster-secrets
