---
# Its own identity: a RoleBinding names it, and the chart may rename its own.
# No pod runs as it; external-secrets requests tokens for the store.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: spindrift-datastore-store
  namespace: external-secrets
