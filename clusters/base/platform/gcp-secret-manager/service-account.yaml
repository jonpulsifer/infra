---
# Its own identity: terraform/gcp/projects/bluenose/iam.tf binds this name, and
# the chart may rename its own. No pod runs as it; external-secrets requests tokens.
apiVersion: v1
kind: ServiceAccount
metadata:
  name: gcp-secret-manager
  namespace: external-secrets
