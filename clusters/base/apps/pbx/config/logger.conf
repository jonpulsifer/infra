; Everything to stdout. The cluster's log path is Vector -> VictoriaLogs; a
; file inside the container is a log that dies with the pod.
[general]
dateformat = %F %T

[logfiles]
console => notice,warning,error
