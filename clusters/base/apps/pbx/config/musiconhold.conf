; Hold music: the stock tracks the image ships. A relative directory is read
; from Asterisk's data directory, which is the image's /var/lib/asterisk.
[default]
mode = files
directory = moh
sort = random
