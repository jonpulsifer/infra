; Hold music: the stock tracks the image ships under /var/lib/asterisk/moh.
; A relative directory is read from Asterisk's data directory, so this names
; no store path.
[default]
mode = files
directory = moh
