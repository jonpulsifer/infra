---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: infra
  namespace: arc
spec:
  interval: 24h
  chartRef:
    kind: OCIRepository
    name: gha-runner-scale-set
    namespace: flux-system
  maxHistory: 2
  install:
    remediation:
      retries: 3
  upgrade:
    cleanupOnFail: true
    force: true
    remediation:
      retries: 3
  uninstall:
    keepHistory: false
  values:
    runnerScaleSetName: infra-${CLUSTER_NAME}
    scaleSetLabels:
      - "${CLUSTER_NAME}"
      - "self-hosted"
    githubConfigUrl: https://github.com/jonpulsifer/infra
    githubConfigSecret: arc
    minRunners: 1
    # dind runs dockerd in the runner's pod, so a job's `services:` ports answer
    # on localhost. The chart adds the sidecar and merges the `runner` container
    # below into its own.
    containerMode:
      type: dind
    template:
      spec:
        containers:
          - name: runner
            image: ghcr.io/jonpulsifer/actions-runner:latest@sha256:428ac7ff17846eb2c6b0bd4d71f899cad7b0d9f7cc53d80a881c7db5d3c37389
            command: ["/home/runner/run.sh"]

