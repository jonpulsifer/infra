apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: iperf3
  namespace: iperf3
  labels:
    app.kubernetes.io/name: iperf3
    app.kubernetes.io/part-of: netbench
spec:
  selector:
    matchLabels:
      app.kubernetes.io/name: iperf3
  template:
    metadata:
      labels:
        app.kubernetes.io/name: iperf3
        app.kubernetes.io/part-of: netbench
    spec:
      # hostNetwork makes every node reachable at <node-ip>:5201 on its VLAN,
      # so netbench's static DNS targets resolve and traffic traverses the real
      # wire (inter-node), the VLAN router (inter-LAN), and the site tunnel
      # (cross-cluster) rather than the Cilium overlay.
      hostNetwork: true
      dnsPolicy: ClusterFirstWithHostNet
      automountServiceAccountToken: false
      # Run a server on every node, including any tainted/control-plane nodes.
      tolerations:
        - operator: Exists
      securityContext:
        runAsNonRoot: true
        runAsUser: 65532
        runAsGroup: 65532
        fsGroup: 65532
        fsGroupChangePolicy: OnRootMismatch
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: iperf3
          image: ghcr.io/jonpulsifer/netbench:latest@sha256:c9ed7c174d5f6dd54614d5f92aeff7218ef9c3c9dc8300c3ae8bc482ad14e46e
          imagePullPolicy: IfNotPresent
          command: ["iperf3", "--server", "--port", "5201"]
          env:
            # iperf3 creates a short-lived stream buffer with mkstemp(3).
            - name: TMPDIR
              value: /tmp
          ports:
            - name: iperf3-tcp
              containerPort: 5201
              protocol: TCP
            - name: iperf3-udp
              containerPort: 5201
              protocol: UDP
          volumeMounts:
            - name: tmp
              mountPath: /tmp
          securityContext:
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop: ["ALL"]
          resources:
            requests:
              cpu: 25m
              memory: 32Mi
            limits:
              memory: 128Mi
      volumes:
        - name: tmp
          emptyDir:
            medium: Memory
            sizeLimit: 64Mi
