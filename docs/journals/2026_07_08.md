- 🎉 **Wiki launched.** `docs/` becomes a Logseq graph published to [wiki.lolwtf.ca](https://wiki.lolwtf.ca) on every merge.
	- Seeded [[Architecture]] (all four layers plus networking, secrets, and GitOps), [[Fleet]], and [[Runbooks]].
	- The first deploy promptly broke — `publish-spa` leaked Node 18 onto `PATH` and wrangler 4 refused. Replaced the renderer with a first-party Bun SSG (`apps/wiki`) the same day. Builds went from ~20 minutes to ~1 second.
